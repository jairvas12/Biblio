spring:
  application:
    name: user-service-test

  datasource:
    url: "jdbc:h2:mem:user_test_db;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: false

    database-platform: org.hibernate.dialect.H2Dialect

  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

jwt:
  secret: "UserServiceTestSecretKey2026Minimum32Characters"
  expiration: 86400000

internal:
  api:
    key: "UserServiceInternalTestKey2026"

springdoc:
  api-docs:
    enabled: false

  swagger-ui:
    enabled: false

logging:
  level:
    root: WARN
    com.library.user_service: INFO